import { StyleSheet } from "react-native";

export default StyleSheet.create({
  root: {
    flex: 1,
    padding: 10,
    alignItems: "center",
    justifyContent: "center",
    paddingTop: 30,
  },
});
